export const BOT_COLOR = {
  embed: '#2BFF00',
  devEmbed: '#eaf85e',
} as const;

export const USER_NOT_REGISTERED_ACTIONS = {
  abort: 'abort',
  askToRegister: 'askToRegister',
  skip: 'skip',
} as const;
